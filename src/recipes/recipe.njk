---
pagination:
  data: recipes
  size: 1
  alias: recipe
permalink: "recipes/{{recipe.id | slugify}}/"
date: Last Modified
---
{% extends "layouts/base.njk" %}

{% block title %}
{{recipe.title}}
{% endblock %}

{% block head %}
<style>
.categories {
	list-style: none;
	display: flex;
	gap: 5px;
	flex-wrap: wrap;
  padding-left: 0;
}

.categories .p-category:not(:last-child)::after {
  content: ",";
}
</style>
{% endblock %}

{% block main %}
    <article class="h-recipe" itemscope itemtype="https://schema.org/Recipe">
        <h1 class="p-name" itemprop="name">{{recipe.title}}</h1>
        <ul class="categories">
            {%- for category in recipe.category %}
            <li class="p-category">{{category}}</li>
            {%- endfor -%}
        </ul>
        <p class="p-summary" itemprop="description">
            {{recipe.summary}}
        </p>
        <section id="ingredients">
            <h2>材料</h2>
            <div>
                <p>
                    <data class="p-yield" itemprop="recipeYield" value="{{recipe.serving}}">{{recipe.serving}}</data>人前
                </p>
                <ul>
                    {%- for ingredient in recipe.ingredients %}
                        <li class="p-ingredient" itemprop="recipeIngredient">
                            {{ingredient.name}} - 
                            <span>{{ingredient.quantity}}</span>
                            {%- if ingredient.note %}
                                <span>{{ingredient.note}}</span>
                            {% endif -%}
                        </li>
                    {%- endfor -%}
                </ul>
            </div>
        </section>
        <section id="instructions" class="u-instructions">
            <h2>つくり方</h2>
            <ol>
                {%- for instruction in recipe.instructions %}
                    <li itemprop="recipeInstructions" itemscope itemtype="https://schema.org/HowtoStep">
                        <span itemprop="text">{{instruction}}</span>
                    </li>
                {% endfor -%}
            </ol>
        <section>
    </article>
    <a href="https://microformats.org/wiki/h-recipe">
        <img src="http://www.boogdesign.com/images/buttons/microformat_enabled.png" />
    </a>
{% endblock %}