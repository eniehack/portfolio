{
  "name": "portfolio",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "dev": "DEBUG=Eleventy* eleventy --watch --serve",
    "build": "rm -rf _site && NODE_ENV=production eleventy"
  },
  "private": true,
  "license": "UNLICENSED",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-plugin-webc": "^0.11.2",
    "@11ty/webc": "^0.11.4"
  }
}
